# 视频存储服务器配置说明

更新时间：2026-08-20

## 1. 服务器用途

这台阿里云 `ECS` 仅用于存储视频文件，并通过公网 URL 提供访问，不负责视频识别计算。

当前业务链路如下：

1. 前端上传视频到服务器
2. 服务器保存视频到指定目录
3. 服务返回可公网访问的视频 URL
4. 后端将该 URL 传给大模型
5. 大模型通过 URL 访问视频并完成分析

## 2. 服务器基础信息

- 云服务类型：`Alibaba Cloud ECS`
- 公网ip：8.156.85.152
- 操作系统：`Ubuntu 22.04 64-bit`
- Web 服务：`Nginx`
- 视频存储目录：`/data/videos/`
- 视频访问路径前缀：`/videos/`


> 说明：公网 IP、域名、SSH 登录用户等信息建议单独补充后再发给具体组员，不建议直接写入公开文档。

## 3. 视频访问规则

Nginx 已将 URL 路径 `/videos/` 映射到服务器目录 `/data/videos/`。

因此：

- 服务器实际文件路径：

```text
/data/videos/{filename}
```

- 对外访问 URL：

```text
http://<公网IP或域名>/videos/{filename}
```

示例：

```text
http://<公网IP或域名>/videos/test.mp4
```

对应服务器中的文件：

```text
/data/videos/test.mp4
```

## 4. 代码侧需要完成的内容

开发组员需要完成以下工作：

- 将前端上传的视频保存到 `/data/videos/`
- 为上传文件生成唯一文件名，避免重名覆盖
- 上传成功后返回该视频的访问 URL
- 后端将该 URL 传给大模型进行视频内容分析

## 5. 推荐返回格式

上传成功后，接口建议返回如下结构：

```json
{
  "fileName": "xxx.mp4",
  "videoUrl": "http://<公网IP或域名>/videos/xxx.mp4"
}
```

## 6. 当前服务器约定

- 固定视频目录：`/data/videos/`
- 固定 URL 前缀：`http://<公网IP或域名>/videos/`
- 当前用途仅为静态视频托管
- 当前不承担视频识别计算
